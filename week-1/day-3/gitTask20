1. <mkdir MACHINE1>

2. created

3. <echo "# MACHINE1" >> README.md>
   <git init>
   <git add .>
   <git commit -m "first commit">
   <git remote add origin https://github.com/yonitermi/MACHINE1.git>

4.  <cd ~/.ssh>
    <ssh-keygen -t rsa -b 4096 -C "yonitermi@gmail.com">
    <eval $(ssh-agent -s)>
    <ssh-add>

5. configured (paste the key created to SSH keys in github)

6. <echo "githubpart" > hello.txt> 
   <git add .>
   git 

7. <git status> staged

8. <cd ..>
   <mkdir MACHINE2>
   <cd MACHINE2>

9. <git clone https://github.com/yonitermi/MACHINE1.git>

10. <cd ../MACHINE1>
    <git push origin master>

11. <git pull origin master> (need to init also because when i cloned   there was mistake)

